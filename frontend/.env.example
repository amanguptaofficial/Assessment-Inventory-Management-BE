# Base URL of the backend API. The frontend reads this at build time (Vite).
# Local docker-compose / dev:
VITE_API_BASE_URL=http://localhost:8000/api

# Production example (set in Vercel/Netlify project settings):
# VITE_API_BASE_URL=https://your-backend.onrender.com/api
